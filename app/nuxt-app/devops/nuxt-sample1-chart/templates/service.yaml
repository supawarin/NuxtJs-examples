apiVersion: v1
kind: Service
metadata:
  name: {{ include "nuxt-sample1-chart.fullname" . }}
  labels:
    {{- include "nuxt-sample1-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nuxt-sample1-chart.selectorLabels" . | nindent 4 }}
